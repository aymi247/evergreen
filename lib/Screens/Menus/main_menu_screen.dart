import 'package:flutter/material.dart';

import 'components/main_menu_body.dart';

class MainMenuScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}